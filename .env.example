# Frontend -> API base URL override (optional in dev because Vite proxies /api)
VITE_SCORE_API_BASE_URL=

# Backend
DATABASE_URL=postgres://postgres:postgres@localhost:5432/wordle
# Optional for hosted Postgres providers that require SSL
PGSSLMODE=require
